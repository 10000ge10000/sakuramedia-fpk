[
  {
    "stepTitle": "快速安装",
    "items": [
      {
        "type": "radio",
        "field": "COMPOSE_PROFILES",
        "label": "部署模式",
        "initValue": "",
        "options": [
          {"label": "轻量模式（PostgreSQL + 后端 + Web）", "value": "light"},
          {"label": "完整模式（额外启用 JoyTag + Qdrant）", "value": "full"}
        ],
        "rules": [{"required": true, "message": "请选择部署模式"}]
      },
      {
        "type": "text",
        "field": "wizard_media_parent",
        "label": "媒体父目录",
        "helpText": "必填绝对路径；不存在的 downloads 和 sakuramedia 子目录会自动创建，容器内统一为 /mnt/media1。",
        "rules": [{"required": true, "message": "请输入媒体父目录"}, {"pattern": "^/.*", "message": "必须填写绝对路径"}]
      },
      {
        "type": "text",
        "field": "wizard_data_dir",
        "label": "自定义数据目录（可选）",
        "helpText": "留空使用应用中心托管目录；填写时请从 fnOS 文件管理器复制绝对路径。"
      },
      {"type": "text", "field": "wizard_api_port", "label": "API 端口", "initValue": "38000", "rules": [{"required": true, "message": "请输入 API 端口"}, {"pattern": "^[0-9]+$", "message": "端口必须是数字"}]},
      {"type": "text", "field": "wizard_web_port", "label": "Web 端口", "initValue": "38080", "rules": [{"required": true, "message": "请输入 Web 端口"}, {"pattern": "^[0-9]+$", "message": "端口必须是数字"}]},
      {"type": "tips", "helpText": "默认登录用户名为 yiwan，密码为 yiwan123。PUID/PGID 默认 1000，时区默认 Asia/Shanghai。qBittorrent 和 Jackett 会在安装后自动发现；完整模式需要下载约 332 MiB 模型。建议首次登录后按需修改固定密码。"}
    ]
  }
]
